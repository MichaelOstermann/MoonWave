export type TrackDetails = {
    addedAt: string
    bitrate: string
    count: string
    duration: string
    path: string
    sampleRate: string
    size: string
}

export type PlaylistDetails = {
    count: string
    duration: string
    size: string
}
